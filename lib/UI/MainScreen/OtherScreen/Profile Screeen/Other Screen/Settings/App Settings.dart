

import 'package:flutter/material.dart';

class App_Settings extends StatelessWidget {
  const App_Settings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}