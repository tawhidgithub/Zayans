import 'package:flutter/material.dart';

class PCFilter extends StatelessWidget {
  const PCFilter({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("PC Filter"),
      ),
    );
  }
}
