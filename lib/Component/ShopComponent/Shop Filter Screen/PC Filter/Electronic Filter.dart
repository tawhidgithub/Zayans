import 'package:flutter/material.dart';

class ElectornicFilter extends StatelessWidget {
  const ElectornicFilter({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Electornic Filter"),
      ),
    );
  }
}
