import 'package:flutter/material.dart';

class LaptopFilter extends StatelessWidget {
  const LaptopFilter({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Laptop Filter"),
      ),
    );
  }
}
