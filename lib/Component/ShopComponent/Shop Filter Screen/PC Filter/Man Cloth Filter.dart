import 'package:flutter/material.dart';

class ManClothFilter extends StatelessWidget {
  const ManClothFilter({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Man Cloth Filter"),
      ),
    );
  }
}
