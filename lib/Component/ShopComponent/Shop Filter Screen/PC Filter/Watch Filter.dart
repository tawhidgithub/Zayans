import 'package:flutter/material.dart';

class WatchFilter extends StatelessWidget {
  const WatchFilter({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Watch Filter"),
      ),
    );
  }
}
